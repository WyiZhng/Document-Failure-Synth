# LLM service configuration
OPENAI_API_KEY=
OPENAI_BASE_URL=
SYNTH_LLM_MODEL=

# Reachable OCR layout-parsing service endpoint
PADDLE_OCR_API_URL=

# Optional image tag override for a prebuilt registry image
SYNTH_IMAGE=document-failure-synth:local
